# Test App

This is a simple test app with very few features. It can only print "Hello, World" to standard output. Its reason to exist is to make a little app, to test if everything is setup up properly on my new PC.


## Using the app
```
>>> python app.py
Hello, World!
```
