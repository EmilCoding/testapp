[build-system]
requires = ["setuptools >= 77.0.3"]
build-backend = "setuptools.build_meta"

[project]
name = "app"
version = "0.1.0"
readme = "README.md"
description = "An application that can print 'Hello, World!' to the console."
requires-python = ">=3.8"
authors = [{name = "ELB"}]
